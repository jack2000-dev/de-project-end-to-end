name: 'de-project-end-to-end'
version: '1.0.0'

profile: 'default'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  de-project-end-to-end:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table