name: 'de-project-end-to-end'
version: '1.0.0'

profile: 'default'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  de-project-end-to-end:
    staging:
      +database: GITHUB_STAGING
      +schema: staging
      +materialized: view
    marts:
      +database: GITHUB_ANALYTICS
      +schema: marts
      +materialized: table