version: 2

sources:
  - name: github_raw
    database: GITHUB_RAW
    schema: GITHUB
    tables:
      - name: repository
      - name: commit
      - name: user
      - name: issue
      - name: pull_request
      - name: user_email

models:
  - name: stg_repositories
    description: "Cleaned repository data from GitHub"
    columns:
      - name: repository_id
        description: "Unique identifier for the repository"
        tests:
          - unique
          - not_null
      - name: repository_name
        description: "Name of the repository"
        tests:
          - not_null
      - name: language
        description: "Primary programming language"

  - name: stg_users
    description: "Cleaned user data from GitHub"
    columns:
      - name: user_id
        description: "Unique identifier for the user"
        tests:
          - unique
          - not_null
  
  - name: stg_user_emails
    description: "Cleaned email data from GitHub"
    columns:
      - name: user_id
        description: "Unique identifier for the user"
        tests:
          - not_null
      - name: email
        description: "Unique email for the user"
        tests:
          - unique
          - not_null